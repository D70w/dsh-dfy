# 大肥鱼角色资产拆分与重组规范

## 1. 目标

本规范约束从一张完整角色图获得可复用静态零件，并由 WhaleRig 在绑定姿势中重组原图、在运行时通过骨骼、网格和弹簧产生动作。

资产不是“按可见轮廓裁出的碎片”。它是具有明确语义所有权、隐藏补全、统一坐标、关节锚点和绘制层级的角色模型输入。

最终运行时输入必须是：

```text
少量静态透明 PNG + Rig + Motion + Physics
```

不得以 GIF、视频、动画 WebP、动作 PNG 序列、Sprite Sheet 或完整人物动作帧作为新 Runtime 的动画来源。

## 2. 不可破坏的不变量

1. 同一角色包内所有零件共享同一画布尺寸和坐标系；当前候选固定为 `1024×1024`、Canvas Y 向下。
2. 零件保留原图坐标，默认重组使用 `drawImage(part, 0, 0)`，禁止通过逐件缩放猜位置。
3. 每个可见像素只有一个语义所有者。隐藏关节重叠可以有意复制，但不得把头发留在手臂、裙片留在腿或皮肤留在服装层。
4. AI 只补原图不存在的遮挡区域。原图已有像素优先直接保留或从错误图层精确迁移。
5. 正式资产不得出现棋盘格假透明、背景色、边缘杂点、白边、黑边、内部半透明或孤立异常像素。
6. 任何修复均生成版本化候选文件，不直接覆盖已验收资产。

## 3. 语义所有权

拆分前先建立像素归属表，而不是先画裁剪框。

| 语义部件 | 可见像素所有者 | 允许的隐藏重叠 | 禁止混入 |
|---|---|---|---|
| 头部与前发 | `head-front` | 颈后发根、脸侧发根 | 手臂、袖子、连衣裙 |
| 下部后发 | `hair-back` | 上部头发内部 | 手、裙摆、鲸尾 |
| 近/远手臂 | `arm-near/far` | 肩根、肘部、袖口 | 发梢、身体布料 |
| 连衣裙 | `dress` | 肩根、髋部、腿根、尾根 | 手和腿的可见轮廓 |
| 近/远腿 | `leg-near/far` | 裙下隐藏大腿根 | 裙片、尾巴 |
| 鲸尾 | `tail` | 裙后隐藏尾根 | 裙摆和头发 |
| 呆毛 | `ahoge` | 头顶隐藏根部 | 头饰背景像素 |

规则：可见像素单一所有权，隐藏连接处保留有界重叠。

## 4. 标准制作流程

### 4.1 锁定母图

- 保存不可修改的原始母图和来源、授权、哈希。
- 记录画布、角色朝向、绑定姿势和角色整体 Pivot。
- 后续 AI 输出只能作为 Donor，不自动成为新的母图。

### 4.2 提取可见区域

- 按语义蒙版提取，不使用纯颜色阈值直接决定最终归属。
- 边界沿真实外轮廓，不能沿遮挡者轮廓截断被遮挡部件。
- 记录每个零件的 Alpha bbox、连通区域数量和异常小组件。

### 4.3 补全被遮挡区域

优先顺序：

1. 从错误图层恢复仍存在的原始像素。
2. 从同源图或同一连续素材中迁移。
3. 使用 AI 局部补全，且锁定画布、身份、比例和未编辑区域。

补全必须覆盖整个语义部件到隐藏根部。例如发梢修复应替换“隐藏发根到卷曲末端”的完整发束，不能把单独末端贴在可见断口上。

### 4.4 Alpha 与边缘清理

- 删除非主语义区域的孤立组件。
- 内部稳定区域恢复为 Alpha 255，边缘保留一至两像素抗锯齿。
- Alpha 0 像素的 RGB 归零，防止纹理过滤产生色边。
- 浅色、深色背景分别检查白边、黑边和半透明脏污。

### 4.5 统一坐标和锚点

每个资产记录：

```text
canvasSize
alphaBbox
semanticOwner
zIndex
parentBone
pivot
jointOverlap
sourceHash
```

不得为“看起来差不多”而独立缩放。确需缩放时必须围绕已确认关节锚点执行，并重新做绑定姿势对照。

### 4.6 绑定姿势重组

按正式 z-order 在共享画布重组。绑定姿势必须先通过，才能制作动作。

重点检查：头与脖子、肩根与袖子、裙下髋部、鲸尾根部、上下后发，以及近远肢体遮挡顺序。

## 5. 动画友好拆分

拆分边界由运动拓扑决定：

- 只绕肩摆动的手臂可以暂时保持完整；需要弯肘时拆为上臂、前臂和手，或使用经过验收的局部网格。
- 腿的髋、膝、踝必须有独立骨骼和连续蒙皮权重。
- 发束、呆毛、裙摆和鲸尾从隐藏根部开始成为完整链，不允许只给末端做弹簧。
- 关节接缝藏在袖口、裙下、头发内部或其他遮挡区，不能在外轮廓上硬拼。

主动画和次级运动严格分离：

```text
Animation Clip
  → Primary Pose
  → Bone World Transform
  → Spring / Secondary Motion
  → Mesh Deformation
  → Render
```

## 6. 验收矩阵

### 6.1 静态验收

- 透明重组和母图叠加；
- 浅色、深色背景；
- 局部接缝五倍放大；
- Alpha bbox、连通区域和异常像素报告。

### 6.2 动态压力验收

- 头发、手臂、双腿和鲸尾分别小幅错位；
- 绑定、前摆、后摆、最大安全弯曲；
- 连续播放至少一个完整循环；
- 0.5×、1×、1.7×速度；
- 主动画暂停后检查弹簧是否独立衰减；
- 实际桌宠尺寸检查可读性和闪烁。

### 6.3 硬失败条件

- 一个部件带走另一个语义部件的像素；
- 静态正确但一运动就露洞、重影或断层；
- 通过缩放掩盖骨骼/锚点错误；
- AI 修改脸部、身份或未授权区域；
- 需要重新生成动作图片才能改变速度；
- 新 Runtime 引用动作帧目录。

## 7. 候选到正式资产

1. 所有生成和修复先进入 `candidate` 目录。
2. 构建机器可读清单并锁定哈希。
3. 完成静态与动态验收产物。
4. 用户明确接受后，生成内容哈希文件并原子提升为正式版本。
5. 正式版本只从严格白名单公开；旧候选与动作参考不能进入安装包。

任何阶段不得混用两个角色版本的零件；若必须保留旧生产基线，应在代码和文档中明确标为 `legacy/reference`。

## 8. 双臂肩—肘—腕拆分实战结论

用户 V1 双臂验证出一条很重要的规则：不能把袖口点误当肘点。肘关节必须先按肩到腕的骨性中心线定位，再决定像素如何分层。

当前采用：

```text
肩骨骼
  └─ 上臂袖
      └─ 肘骨骼
          └─ 前臂袖
              └─ 腕骨骼
                  └─ 袖口与手
```

- 近侧锚点：肩 `(470,535)`、肘 `(517,590)`、腕 `(563,642)`。
- 远侧锚点：肩 `(286,550)`、肘 `(259,592)`、腕 `(229,638)`。
- 上臂袖、前臂袖、袖口与手的可见像素互斥且并集等于原完整手臂；绑定姿势重组差异为 0 像素。
- 肘部补片不应向原轮廓外“画一个圆筒”，否则弯曲后会出现蓝色鼓包。正确做法是在原袖内部复制一小段相邻袖布作为交叉重叠，绑定时完全被覆盖，关节转开时才填缝。
- AI 生成结果必须先验真。本轮两张 Donor 均因尺寸变为 `1254×1254`、无 Alpha 且把棋盘格画入 RGB 而被拒绝；生成结果不得因为“看起来像透明”就进入资产包。
- 纯刚性分片只适合小角度跑步摆臂。若以后需要大幅屈肘，应在肘部增加局部 Mesh 权重过渡，不能继续扩大隐藏补片掩盖形变。

可重复构建入口为 `scripts/build-user-v1-arm-rig-assets.py`，机器报告为 `character-packs/default-whale/source/bind-pose-v3/reports/arm-rig-v1.json`。

## 9. V1 正式提升记录（2026-08-21）

- 正式清单：`character-packs/default-whale/runtime/rig2/manifest.d4cc19463f1e.json`。
- 运行时包含 19 张静态语义纹理，`animationFrames = 0`。
- 头发、鲸尾、呆毛进入网格；双腿进入三骨骼 CPU 蒙皮；双臂进入肩—肘—腕层级。
- 生产包明确排除旧 motion atlas、`motions/` 与完整人物动作帧。
- 112px 局部截图未发现白边、黑洞、裙片混腿、头发绑手、明显接缝或异常像素；左右实时跑步和完整自主故事已在隔离 DSH profile 复验。
