# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是使用 DeepSeek Harness 工作、学习或编程的人。他们长时间停留在 Harness Web 工作区，希望无需离开当前任务，就能看懂系统正在工作、完成、出错或空闲，同时获得轻量、可爱的陪伴。

## Product Purpose

dsh-dfy 在 DeepSeek Harness 工作区边缘提供一位可互动、可记忆、与真实工作状态联动的“大肥鱼”工位搭子。成功不是增加一个功能面板，而是让用户一周后因她能安静陪伴、准确回应工作状态并记得共同经历而舍不得关闭。

## Positioning

本产品不是播放循环动画的泛用桌宠，也不是以签到、衰减数值和商城为主的传统电子宠物。它的独特机制是把 Harness 的真实工作事件转译成“大肥鱼”的角色表演：认真干活、完成后邀功、随后理直气壮摸鱼；语言嘴硬，表情、呆毛、耳鳍和鲸尾表达真实情绪。

## Operating Context

- 运行在 DeepSeek Harness Web Client 的 `shell.overlay` 中，默认位于工作区右下角安全位置。
- 以可安装的 Host/Browser 双面 Cordis 插件接入，不修改 Harness agent loop，不占用 `root`，不建立独立 React 根节点。
- 用户的首要任务始终是使用 Harness；桌宠必须可瞥见、可隐藏、可暂停、可进入安静模式。
- 宠物响应 `turn/start`、`step/start`、`tool/call`、`tool/result`、`turn/end` 以及实时 running/idle 状态，但不读取或展示代码、Prompt、终端输出和文件正文。
- 用户可点击、摸头、拖拽和投喂；长期关系通过共同工作、错误后恢复、回归和纪念日积累。

## Capabilities and Constraints

- 核心价值顺序已确认：工位搭子与工作联动 > 角色小剧场 > 轻量养成。
- 正常工作时保持低打扰：大多数反馈通过动作完成，气泡只在关键变化、低频彩蛋和用户主动互动时出现。
- Harness 空闲时，大肥鱼拥有可中断的自主生活：围绕固定工位产生意图、在有限领地活动并形成可被用户轻度介入的小剧场；任何工作事件都优先打断摸鱼行为。
- 长期留存主要来自共同记忆；动作、装扮和食物是奖励，热点梗只作为可替换调味。
- 默认角色可视高度约 96–112px，支持缩放；小尺寸下优先保证脸、呆毛、耳鳍和鲸尾可读。
- 产品以桌面 Web 为首要场景；窄窗口和触摸设备提供安全降级，不承诺移动端具备完整的指针拜访与散步体验。
- 长期仅持久化饱食度、心情、好感度和精力；工作专注与压力属于瞬时上下文。
- 离线不惩罚、不扣好感、不致病或死亡；宠物状态不得阻塞 Harness 功能。
- 默认角色采用项目自有的轻量实时动画运行时 WhaleRig2：19 张静态语义零件通过骨骼层级、关键帧曲线、CPU Mesh 与独立 Spring 在运行时生成待机、工作和跑步动作；所有动作共享舞台 Pivot、状态混合、镜像与外层移动控制器。项目不引入 Cubism Core、完整刚体物理引擎或任何动作序列帧。
- 白饭小剧场具有克制的跨活跃日因果：被发现偷吃后，后续活跃日可能先发生饭碗意外，再出现收拾与补饭；线索七个活跃日内过期、每个结果只消费一次，仍服从工作优先、每天最多两次和随时中断。
- WhaleRig 是插件内部实现，不成为 Harness 公共 Service，不改变 Session/Event 协议；行为状态机只输出与渲染技术无关的动作意图。
- 首版不做完整聊天机器人、复杂商城、联网排行榜、强社交、外部角色包或模型生成台词。
- DSH 技术基线为 `0.1.0-rc.5`；Host/Browser 双面 Cordis 插件、Session Projection、Settings namespace、Storage Domain 与 Client Store 的边界以详细设计文档为准。

## Brand Commitments

- 项目名：dsh-dfy。
- 默认角色：社区二创语境中的蓝色“大肥鱼”，定位为“嘴硬但会干活的工位搭子”。
- 核心人格：聪明可靠、爱吃白饭、会摸鱼、嘴硬心软、不承认胖；摸鱼是表演，能干是底座。
- 用户关系：平等的损友、同事与长期搭档；默认不称“主人”，关系成长不等于服从成长。
- 核心节奏：“干活—邀功—摸鱼”；禁止用“潜航节律”或深海神秘叙事替代。
- 稳定人格与梗层分离：Token、鱼片、模型版本笑话只能作为低频、可替换、可关闭内容。
- 角色和文案基线见 `dsh-dfy_角色设定文档_v0.1.md`。

## Evidence on Hand

- 已确认视觉基准：`artifacts/whale-pet-final-character-preview-v1.png`；透明处理源位于 `character-packs/default-whale/source/canonical-transparent.png`。
- 原始“鲸鱼娘形象”作者：上善无形（Bilibili UID 4456176）；原帖：<https://www.bilibili.com/opus/1231977657712771073>，声明采用 CC BY-NC-SA 4.0。
- 社区 Q 版与动态比例参考：<https://www.bilibili.com/video/BV1ykb16GEHg/>；视频和用户截图仅作参考，不嵌入发行包。
- 详细署名、改动和混合许可边界：`ASSETS_LICENSE.md` 与 `character-packs/default-whale/SOURCE.md`。
- 第三方插件工程参考：[`zhu1090093659/dsh-web-ui`](https://github.com/zhu1090093659/dsh-web-ui) 的 `@linxin666/dsh-pet`；只借鉴双面包、bundle patch、Host route、Settings 与资源组织，兼容性仍以本地 DSH `0.1.0-rc.5` 为准。
- 已有产品和技术基线：`dsh-dfy_详细设计文档_v0.1.md`。
- 角色研究和来源链接：`dsh-dfy_角色设定文档_v0.1.md`。
- 当前默认角色衍生素材按 CC BY-NC-SA 4.0 非商业、署名、相同方式共享；代码仍为 MIT。后续不得把角色素材误标为 MIT，也不得删除署名与改动记录。
- 当前没有用户研究数据或低端设备性能实测；后续工作不得虚构指标或用户反馈。

## Implementation Snapshot (2026-08-21)

- 已接入用户确认的 Q 版女仆鲸鱼娘：高分辨率视觉基准、透明处理源和 224×224 运行时 PNG 均在项目内留档；桌面实际显示基线为 112×112px。
- 已实现首个自主“捉蝴蝶”闭环：发现、意图、尝试、结果、整理和回工位均可被 Harness 开工、直接互动、键盘焦点、页面隐藏、安静模式和 reduced motion 安全打断。
- 已停止并退出生产所有动作序列帧、Sprite Sheet、GIF、视频和完整人物动作帧。当前 WhaleRig2 仅使用 19 张静态语义 PNG，由骨骼层级、实时关键帧插值、CPU Mesh 和固定步长 Spring 生成每一帧；900ms Run 具备双腿交替、反向摆臂、身体起伏、头部补偿及独立头发/鲸尾/呆毛余动，左右移动镜像完整运行时结果。
- 蝴蝶是独立 DOM/SVG Actor，拥有由种子决定的 60FPS 飞行曲线与独立翅膀频率；角色图集不再包含蝴蝶。追逐、指针拜访和回工位由同一个移动控制器组合公共 Clip。
- 已实现同源多标签唯一表演者：优先 Web Locks，降级为 BroadcastChannel + localStorage 短租约；缺少跨标签能力时不持久化故事结果。
- 已实现低频指针拜访：指针静止至少 2 秒、与 homeAnchor 共享视口边缘带、移动不超过 180px 且整条路线不命中通用交互元素时才会发生；点击、敲键、快速移动、窗口变化或 Harness 开工都会令她立即回撤/清理。
- 已实现可聚焦菜单中的“叫她过来/回工位休息”：召唤会进入最多 6 秒的安全目标等待，回工位会在本页面停留期间阻止自动外出；用户主动召唤属于 L4 互动，不占每日自主故事额度，也不写长期故事记忆。
- 已实现可聚焦菜单中的“追赶蝴蝶”：用户可立即播放由公共动作和独立蝴蝶编排的完整故事；手动演出固定成功，不占每日故事次数、不修改连续扑空统计，也不写长期故事记忆。
- 已实现打盹故事：她在 homeAnchor 拿出独立抱枕层并进入静止睡眠；只有安全睡眠结果阶段恢复角色热点，指针靠近或直接点击会进入“被发现后嘴硬否认”的 `seen` 收尾，工作等高优先级输入仍立即清理。
- 已实现偷吃白饭：角色复用正式 Run 接近独立饭碗，饭碗用反向位移补偿保持世界坐标稳定；指针靠近或点击进入被发现、嘴硬否认和回工位分支，不另建自主系统。
- Host 已直接监听提交后的 `turn/start`/`turn/end`，用 `session:<id>:<seq>` 收据幂等结算完成回合、结束原因和多会话区间并集工作分钟；Browser 命令不能伪造该奖励。
- 快捷菜单已提供工位小账本：展示四项状态、当天 2–5 句事实型角色摘要、最多七天记录与三个共同成就；打开时刷新 Host 快照，不常驻轮询。
- 小账本已提供二次确认的隐私清理：仅删除日期账页、月度账页和小剧场分支记忆；默契、四项状态、成就、累计工作、活跃天数、每日频控与 Host 幂等收据均保持不变，失败时保留原数据并允许重试。
- 好感度已落为初见/熟悉/亲近/信赖/老朋友五个可见阶段，而不是孤立数值；自动指针拜访在熟悉后解锁，并随阶段把角色边缘距离从 64px 逐步收近到不低于 40px。互动、白饭和共同工作权重只选择审核过的熟悉台词，不改变系统事实或核心人格。

## Product Principles

1. Harness 工作优先：角色存在感服务于工作，不与工作争夺注意力。
2. 状态准确、表演有性格：系统事实不能编造，笑点来自反差而非错误信息。
3. 安静但有生命：用户不需要操作她，也能从低幅动作感到陪伴。
4. 行为有因果：自主动作必须有意图、过程、结果和角色化收尾，而不是无意义随机播放。
5. 彼此养活：用户可以投喂她，她也通过完成反馈、休息提醒和共同记忆照顾用户。
6. 长期关系胜过内容堆积：专属共同经历比货币、签到和热点梗更重要。
7. 宿主边界清晰：只消费 Harness 正式插件接口和已提交事实；插件卸载后撤销全部注册、监听、动画与资源，不让桌宠成为宿主稳定性的风险源。

## Accessibility & Inclusion

- 遵守 `prefers-reduced-motion`，提供全局暂停、隐藏和安静模式。
- 关键状态不能只靠颜色或快速动画表达；详情面板提供简短文字状态。
- 宠物入口和菜单支持键盘操作、可见焦点及可读 `aria-label`。
- 音效默认关闭，不能作为唯一反馈。
